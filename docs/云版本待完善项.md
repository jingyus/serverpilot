# 云版本待完善项

> 基于 CLOUD_TASK_QUEUE、云服务开发指南、多租户架构补充 整理。  
> 后端 cloud-001～cloud-018 已按计划实现，本文档列出尚未覆盖或可增强的部分。

---

## 一、当前已完成（后端/API）

- **cloud-001～011**：AI 配额、Stripe 计费、Agent 认证、智能模型路由、CostTracker、Cloud AI Provider  
- **cloud-012**：Cloud 用户注册（tenant + owner + JWT 含 tenantId）  
- **cloud-014**：verify-tenant 中间件  
- **cloud-015**：skill_executions 表 + Repository  
- **cloud-013**：Usage API（summary / history / quota）  
- **cloud-016**：log-scanner Skill  
- **cloud-017**：security-scanner Skill  
- **cloud-018**：Cloud 入口集成（index / billing / analytics 导出）

---

## 二、待完善项（按优先级）

### 1. Dashboard 前端（高）— 已实现 ✅

| 项 | 说明 | 状态 |
|----|------|------|
| **Billing 页** | 订阅管理、计划列表、当前订阅、取消 | 已实现 `packages/dashboard/src/pages/Billing.tsx` |
| **Usage 页** | 本月摘要、历史趋势、配额、Skills 统计 | 已实现 `packages/dashboard/src/pages/Usage.tsx` |
| **API 客户端** | 请求时自动带 `X-Tenant-ID` | 已实现：`getTenantId`/`setTenantId`，login/register 存 tenantId，`apiRequest` 带 header |
| **配额展示** | Chat/设置等处剩余 AI 配额、升级入口 | 可选：Usage 页已展示配额；Chat 内可后续加 |

Server 已挂载 Cloud 路由：postgres 模式下 `mountCloudRoutes(apiApp, requireAuth)` 挂载 `/api/v1/usage`、`/api/v1/billing`，中间件为 requireAuth → cloudContextFromDb → verifyTenant。

---

### 2. 日志/安全 Skill 的运行时对接（中）

| 项 | 说明 |
|----|------|
| **log-scanner** | `scanLogs` 的 `logFetcher` 当前可为空（占位）。需由上层注入：通过 server/agent 在目标机执行 `tail/cat` 等拉取 syslog、nginx error、mysql error。 |
| **security-scanner** | `securityAudit` 的 `commandRunner` 需由上层注入：在目标机执行 apt/dpkg/sshd_config/iptables/last/ps/netstat 等命令。 |
| **定时任务** | `enableAutoScan` + `runScheduledScans` 已实现，需在 server 或独立 worker 中按小时调用并传入各 server 的 `ScanLogsContext`。 |

建议：在 server 或 cloud 入口提供「在指定 server 上执行命令」的接口，并注入到上述两个 Skill。

---

### 3. 邮件与通知（中）

| 项 | 说明 |
|----|------|
| **欢迎邮件** | 注册成功后发送（含 dashboard 链接、入门指引）。当前可先打日志，后续接 SMTP/SendGrid。 |
| **订阅取消/付款失败** | Webhook 处理中已打日志，可增加邮件模板与 SMTP 发送（付款失败、即将取消、已降级 Free 等）。 |

---

### 4. 更多 Skills（开发指南 P1/P2）

| 项 | 说明 | 优先级 |
|----|------|--------|
| **性能优化 Skill** | 分析 7 天性能指标，识别瓶颈（CPU/内存/磁盘） | P1 |
| **成本优化 Skill** | 分析云账单（AWS/gcloud），给出节省建议 | P1 |
| **备份顾问** | 分析服务器状态，基于 3-2-1 原则给备份策略建议 | P1/P2 |

实现方式可参考 log-scanner/security-scanner：数据收集 → Cloud AI 分析 → 结构化报告 → 写入 skill_executions。

---

### 5. 多租户与入口（可选）

| 项 | 说明 |
|----|------|
| **子域名隔离** | 按 tenant slug 提供 `https://${tenantSlug}.serverpilot.io`，路由到同一 Dashboard 并自动带 tenant 上下文。多租户架构补充中为「可选」。 |
| **Server 挂载 Cloud 路由** | 将 `createUsageRoutes`、`createBillingRoutes` 挂到 server 的 Hono 应用，并配置 requireAuth + verifyTenant；确保 Cloud 注册登录流程与现有 auth 衔接。 |

---

### 6. P2 企业功能（开发指南）

| 项 | 说明 |
|----|------|
| **LiteLLM Gateway** | 统一 AI 网关、多模型路由与降级、成本追踪（见开发指南「AI Gateway 部署」）。 |
| **SAML SSO** | Enterprise 使用 Okta/Google Workspace/Azure AD 登录（如 passport-saml）。 |
| **自定义 Skills** | Enterprise 用户可自定义 Skill prompt 或扩展执行逻辑。 |

---

### 7. 任务队列与文档同步（低）

| 项 | 说明 |
|----|------|
| **CLOUD_TASK_QUEUE.md** | 将 cloud-012 标为 completed，cloud-013～018 标为 completed，与当前实现一致，避免误判为未做。 |
| **开发指南** | 若 P0/P1 列表中有「用户注册、Usage、verifyTenant、log-scanner、security-scanner、入口集成」，可标注为已实现。 |

---

## 三、建议实施顺序

1. **短期**：Dashboard 前端（Usage 页 + Billing 页 + API 客户端统一带 `X-Tenant-ID`）→ 用户可完整使用计费与使用量。  
2. **中期**：log/security 的 logFetcher、commandRunner 对接 + 定时巡检；欢迎邮件/付款失败邮件（SMTP）。  
3. **后续**：性能/成本/备份 Skill；子域名与 SSO、LiteLLM、自定义 Skills 等 P2。

---

## 四、总结

- **后端/API**：Cloud 核心（注册、多租户、配额、计费、Usage、log-scanner、security-scanner、入口集成）已实现并可测试。  
- **主要缺口**：前端 Dashboard（Billing/Usage + 统一 tenantId）、Skills 在真实环境中的命令/日志拉取、邮件通知与可选 P2 企业功能。按上表逐项补齐即可持续完善云版本。
